const getters = {
	/**
	 * @description 로딩 완료 상태 조회
	 * @returns state.screen.loadingComplete 로딩 완료 상태
	 */
	loadingComplete: (state) => state.loadingComplete,

	/**
	 * @description 로딩 완료 상태 조회
	 * @returns state.screen.loadingComplete 로딩 완료 상태
	 */
	boardViewData: (state) => state.boardViewData,
};

export default getters;
